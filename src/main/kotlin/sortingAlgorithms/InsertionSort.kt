package sortingAlgorithms

import testArrayForSorting

fun main(){
    println(insertionSort(testArrayForSorting).contentToString())
}

fun insertionSort(arr: Array<Int>):Array<Int>{

    for (i in 1..arr.lastIndex){
        var newElement = arr[i]

    }

    return arr
}